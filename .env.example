PORT=3000

# ==================== 加密配置 ====================
# 账号 Token 加密密钥（强烈推荐设置）
# 运行 node generate-secret.js 生成 64 位十六进制密钥
# 设置后，账号的 API Token 将使用 AES-256-GCM 加密存储
ACCOUNTS_SECRET=

# ==================== 预配置账号 ====================
# 多设备同步账号配置（可选）
# 格式: 账号名称:API_TOKEN
# 多个账号用逗号分隔
# 示例: ACCOUNTS=主账号:sk-xxxxxx,备用账号:sk-yyyyyy
ACCOUNTS=

# ==================== PostgreSQL 数据库（可选） ====================
# 配置后将使用数据库存储代替文件存储
# 格式: postgres://用户名:密码@主机:端口/数据库名
# 示例: DATABASE_URL=postgres://zeabur:password@localhost:5432/zeabur_monitor
DATABASE_URL=

# 数据库 Schema（可选）
# 某些云数据库（如 Filess.io）不允许使用 public schema
# 需要设置为数据库用户名同名的 schema
# 示例: DATABASE_SCHEMA=zeabur_monitor_givinggift
DATABASE_SCHEMA=

# 数据库 SSL 配置（可选）
# 设置为 false 可禁用 SSL（仅用于本地或内部网络）
DATABASE_SSL=true

# ==================== Redis 缓存与 Session（可选） ====================
# 配置后将使用 Redis 存储 Session 和缓存数据
# 支持重启后保持登录状态，并缓存 API 响应提升性能

# Redis 连接地址
# 格式: redis://[:密码@]主机:端口[/数据库号]
# 示例: REDIS_URL=redis://localhost:6379
# TLS 示例: REDIS_URL=rediss://user:password@host:6380
REDIS_URL=

# Redis TLS 配置（可选）
# 使用 rediss:// 协议时自动启用 TLS
# 或显式设置为 true/false
# 示例: REDIS_TLS=true
REDIS_TLS=

# Redis TLS CA 证书路径（可选）
# 用于验证服务器证书
# 示例: REDIS_TLS_CA=/path/to/ca.pem
REDIS_TLS_CA=

# Redis TLS 证书验证（可选）
# 设置为 false 可跳过证书验证（仅用于开发环境）
# 示例: REDIS_TLS_REJECT_UNAUTHORIZED=false
REDIS_TLS_REJECT_UNAUTHORIZED=true

# Redis 连接超时（毫秒，可选）
# 默认: 10000
REDIS_CONNECT_TIMEOUT=

# Redis 命令超时（毫秒，可选）
# 默认: 5000
REDIS_COMMAND_TIMEOUT=

# ==================== 告警通知（可选） ====================
# 额度预警阈值（美元），低于此值时发送通知
# 默认: 1.0
QUOTA_WARNING_THRESHOLD=1.0
